[pytest]
testpaths = ./testcase
python_files = test_*.py
python_functions = test_*
markers =
    skip: mark test to be skipped
    dangerous: mark test as tests that might lead to a crash, avoid running them as default